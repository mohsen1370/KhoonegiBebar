package ir.ghaza_khoonegi.www.khoonegibebar;

import android.content.Context;
import android.support.v4.content.res.ResourcesCompat;

import java.util.ArrayList;
import java.util.List;

import ir.ghaza_khoonegi.www.khoonegibebar.Datamodel.FoodModel;

public class DataFakeGenerator {
    /*
    public static List<FoodModel> geFoodModels(Context context){
        List<FoodModel> foodmodels= new ArrayList<>();
        for(int i=1;i<=10;i++)
        {
            FoodModel foodmodel=new FoodModel();
            foodmodel.setId(i);
            foodmodel.setFoodtitle("نام غذا");
            foodmodel.setCheftitle("نام سرآشپز");
            foodmodel.setPricetitle("12,000 تومان");
            foodmodel.setFoodimage(ResourcesCompat.getDrawable(context.getResources(),R.drawable.background_food_item,null));
            foodmodels.add(foodmodel);
        }
        return foodmodels;
    }
    */
}
